#include <stdio.h>
/**
 * main - main
 * Return: 0
 */
int main(void)
{
	long prime_num = 612852475143;
	long divisor = 2;

	while (num != 0)
	{
		if (num % divisor == 0)
		{
			num /= divisor;
			large_num = divisor;
		}
		divisor += 1;
	}
	return (0);
}
